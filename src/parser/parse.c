#include "minishell.h"

// to do
t_ast *parse_expression(t_list **tokens)
{
	return (t_ast *)(tokens);
}

t_ast *parse(t_list *tokens)
{
	(void)tokens;
	// return parse_expression(&tokens);
	return (NULL);
}